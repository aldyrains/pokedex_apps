import 'package:flutter/material.dart';

class PokemonInfoCardBaseStat extends StatelessWidget {
  const PokemonInfoCardBaseStat({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("base stat"),
      ),
    );
  }
}
